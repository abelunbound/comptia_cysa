[pytest]
# Keep Playwright out of this suite. --timeout dumps a stack instead of sitting
# until the job cap (CI stdout is not a TTY, so a hung first test looks like
# "collected 48 items" with no names).
addopts = -p no:playwright -p no:base_url --timeout=30
